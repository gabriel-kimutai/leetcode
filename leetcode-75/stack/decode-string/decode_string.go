package decodestring


func decodeString(s string) string {
    var decodedString string
    for _, c := range s {
        switch c {
        case "[]":
            
        }
    }

    return decodedString
}
