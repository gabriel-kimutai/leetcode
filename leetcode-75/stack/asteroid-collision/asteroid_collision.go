package asteroidcollision

import (
	"fmt"
	"math"
)


func asteroidCollision(asteroids []int) []int {
	var asteroidStack []int

	for i := range asteroids {
		size := math.Abs(float64(i))
		if i < 0 {
			direction = -1
		} else {
			direction = 1
		}

	}
}
